<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.sinchon.mapper.user">
	<!-- 아이디 중복체크 -->
	<select id="idCheck" parameterType="com.project.sinchon.config.security.auth.User" resultType="Map">
		SELECT user_ID
		FROM user
		WHERE user_ID = #{user_ID};
	</select>

	<!-- 회원가입 : 사용자 정보 Insert -->
	<insert id="registerUser" parameterType="com.project.sinchon.config.security.auth.User">
		INSERT INTO user(user_ID, email, pwd)
		VALUES (#{user_ID}, #{email}, #{pwd});
	</insert>
	
	<!-- 로그인 : 사용자가 입력한 정보와 비교할 정보 Select해서 반환 -->
	<select id="findByUserId" resultType="com.project.sinchon.config.security.auth.User">
		SELECT user_ID, pwd, roles
		FROM user
		WHERE user_ID = #{user_ID}
	</select>
	
	<!-- 예약신청시 사용자가 입력한 정보 저장 -->
	<update id="updateUserDetails" parameterType="com.project.sinchon.dto.ApplyReservationDTO">
		UPDATE user
		SET	firstname = #{firstname},
			lastname = #{lastname},
			sex = #{sex},
			country = #{country},
			age_group = #{age_group},
			NA_foods = #{NA_foods},
			update_date = NOW()
		WHERE user_ID = #{user_ID}
	</update>
	
	<!-- MyPage에서 회원정보 수정 -->
	<update id="updateUserProfile" parameterType="com.project.sinchon.dto.UserDTO">
		UPDATE user
		SET	firstname = #{firstname},
			lastname = #{lastname},
			sex = #{sex},
			country = #{country},
			age_group = #{age_group},
			NA_foods = #{NA_foods},
			update_date = NOW()
		WHERE user_ID = #{user_ID}
	</update>
	
	<select id="getUserDetails" resultType="com.project.sinchon.dto.UserDTO">
		SELECT 	firstname
			  , lastname
			  , sex
			  , country
			  , age_group
			  , NA_foods
			  , reg_date
			  , update_date
		FROM user
		WHERE user_ID = #{user_ID}
	</select>
</mapper>